# Alle One — Frontend (Web)

Este diretório contém o **frontend web** do projeto **Alle One** (Next.js).

## Documentação

A documentação completa do projeto (como rodar, variáveis de ambiente, integrações, riscos e roadmap) está no README da raiz:

- **`../README.md`**

## Rodando localmente

```bash
npm install
npm run dev
```

Por padrão:

- **Web**: `http://localhost:3000`

