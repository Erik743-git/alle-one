# Auto detect text files and perform LF normalization
* text=auto

# Nginx na VM Linux nao parseia BOM/CRLF (unknown directive "#")
deploy/*.conf text eol=lf

# Hooks / shell scripts always LF (Windows CRLF quebra husky)
.husky/** text eol=lf
scripts/*.sh text eol=lf
