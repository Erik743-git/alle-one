# Alle One — Backend (API)

Este diretório contém a **API** do projeto **Alle One**.

## Documentação

A documentação completa (arquitetura, como rodar, variáveis de ambiente, Prisma/migrações/seed, riscos e roadmap) está no README da raiz:

- **`../README.md`**

## Atalhos de execução

```bash
npm install
npm run start:dev
```

Após subir:

- **API**: `http://localhost:3003`
- **Swagger**: `http://localhost:3003/docs`

