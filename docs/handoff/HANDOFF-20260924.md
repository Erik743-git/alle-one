# Handoff — Alle One, 23 e 24/09/2026

Para a próxima sessão. **Nada do que está aqui foi testado numa tela.** Os
testes automatizados passam, mas nenhuma pessoa abriu nada disso rodando.
O primeiro passo é o deploy da teste e a seção 3.

---

## 1. Regras que valem daqui para frente

- **Todo trabalho novo nasce da branch que está na teste**
  (`teste/integracao-20260923`), nunca do commit de produção. Para o Erik a
  teste é a próxima produção: ela vai inteira para prod. Isso **revoga** a
  regra do handoff anterior de manter frentes separadas.
- **Não falar de deploy em produção** sem o Erik pedir. O fluxo é: tudo na
  teste → testar → só então prod.
- Funcionalidade nova: **desenhar com o Erik antes de codar.** Correção de
  bug e pedido pequeno: fazer direto.
- Dar sinal de progresso em tarefa longa.
- Sem `Co-Authored-By` nem "Generated with Claude Code" em commit ou PR.
- Nunca `pm2 restart` na API de produção (`pm2 delete` + `pm2 start`).
- Não rodar `prettier --write` nem `prisma format` em arquivo inteiro: os dois
  reformatam centenas de linhas e escondem a mudança real (aconteceu hoje,
  foi desfeito).

## 2. Onde está cada coisa

| | |
|---|---|
| Teste | `teste/integracao-20260923`, ponta **`1833196`** (ou o commit deste handoff, logo acima) |
| Produção | `fix/infra-banco-e-mesa` em **`c40625b`** — confirmado na VM em 23/09. **Não foi tocada.** |
| Deploy da teste | ver seção 3.0 |

A teste está **à frente** do que está rodando lá: o último deploy da teste foi
antes de tudo abaixo. **Três migrations novas** esperando:
`20260923190000_mural_reconhecimento`, `20260923200000_mural_reacoes_visitas`,
`20260924090000_escala_plantao`.

Branches que existem no `origin` com o trabalho de hoje (todas já mescladas
na teste): `fix/infra-banco-e-mesa`, `feat/relatorio-visualizar-pdf`,
`feat/mural-reconhecimento`, `feat/agendas-escala`.

---

## 3. O que testar (na teste)

### 3.0 Deploy

```bash
sudo -u alleone -i env ALLEONE_BRANCH=teste/integracao-20260923 bash /home/alleone/teste/deploy/scripts/pos-deploy-alleone-teste.sh
```

E o nginx, senão o visualizador de PDF continua bloqueado (a política vem
dele, não do Next). Antes, achar onde o snippet está:

```bash
grep -rn "frame-src" /etc/nginx/
```

Copiar `deploy/nginx-alleone-csp-html.snippet.conf` para lá,
`sudo nginx -t` e `sudo systemctl reload nginx`.

E o modelo de e-mail de comunicação (sem isso o texto antigo continua):

```bash
cd /home/alleone/teste/backend && npx ts-node --transpile-only prisma/scripts/atualizar-template-comunicacao.ts
```

(sem `--aplicar` só mostra; com `--aplicar` grava; recusa se alguém editou o
modelo na tela de E-mail)

### 3.1 Visualizador de relatório em PDF — `ERR_BLOCKED_BY_CSP`
- [ ] Gerar relatório, clicar no olho: o PDF abre no diálogo (antes: tela
      "Está bloqueado").
- [ ] Se continuar bloqueado: o nginx não foi recarregado com o snippet novo
      (`frame-src 'self' blob:`).

### 3.2 Rendimento (menu Apontamentos, `/apontamentos`) — comunicação sumiu

Só a folha de horas do colaborador. **Na lista de apontamentos dentro de um
ticket as comunicações continuam aparecendo** — o filtro vive só em
`rendimento.service.ts`.

- [ ] Um dia com comunicação (`13:41 – 13:41 · 00:00`): a linha não aparece.
- [ ] O total de horas do dia **não mudou** (a linha valia zero).
- [ ] Dentro de um **ticket**, a comunicação **continua** na lista.
- [ ] Na tela de apontamentos **da empresa** a comunicação **continua**
      aparecendo (decisão: lá quem olha é o cliente).

### 3.3 Pop-up de atenção da comunicação
- [ ] Abrir chamado com atenção de texto longo: o checkbox "Não exibir
      novamente" e o botão "OK, li a atenção" ficam **sempre visíveis**; só o
      miolo rola.
- [ ] Modal mais largo e mais baixo que antes.
- [ ] Rodapé sem faixa sobrando embaixo e sem botão colado na borda.

### 3.4 E-mail de comunicação enxuto
- [ ] Depois do script da 3.0: fazer uma comunicação e ver o e-mail. Sem a
      descrição do chamado repetida, sem bloco "quem apontou / quando".
- [ ] Assunto: `Chamado #N — título` (sem horário).
- [ ] **Responder** esse e-mail: a resposta tem que voltar para o mesmo
      chamado (o `#N` do assunto é o que liga).

### 3.5 Ler apontamento de outra pessoa (tela do chamado)
- [ ] Logado como alguém que **não** apontou: clicar na linha abre a leitura,
      com descrição grande e anexos.
- [ ] Logado como quem apontou (ou responsável): clicar continua abrindo a
      **edição**, como antes.
- [ ] Testar com usuário não-admin também.

### 3.6 Guias
- [ ] Arrastar uma guia para outra posição.
- [ ] Fechar um chamado: a guia dele mostra contagem (`10s`, `9s`…) e fecha
      sozinha.
- [ ] Clicar na guia durante a contagem **cancela** o fechamento.
- [ ] F5 durante a contagem: a guia volta sem contagem.
- [ ] Se 10s parecer pouco, é a constante `TAB_AUTO_CLOSE_SECONDS` em
      `frontend/components/layout/portal-tabs-provider.tsx` (o Erik tinha
      sugerido 30s; ficou 10s com contagem visível — confirmar com ele).

### 3.7 Mural (menu → Mural)
- [ ] Criar bilhete com cor, destinatário e anônimo.
- [ ] Arrastar o **próprio** bilhete; recarregar a página: ficou no lugar.
- [ ] Bilhete de outra pessoa **não** arrasta; clicar abre de perto.
- [ ] Bilhete anônimo: **ninguém** vê o autor, nem admin.
- [ ] Editar o próprio; tirar o próprio; admin tira o de outro.
- [ ] Reação: clicar liga, clicar de novo desliga; contador certo.
- [ ] Selo "novo": abrir o mural, outra pessoa prega um bilhete, reabrir —
      aparece "novo". Na primeira visita **nada** aparece como novo.
- [ ] Destinatário recebe aviso no **Correio**.
- [ ] Admin vê o painel "Mural do mês"; colaborador não vê.
- [ ] Cliente **não** vê o item no menu nem acessa `/mural`.

### 3.8 Agendas → Escala (menu → Agendas)
- [ ] Aba **Plantão** igual à tela de Plantão antiga (Outlook).
- [ ] Admin: "Nova regra" — pessoa, especialidade, 22:00 às 06:00, seg e qua.
- [ ] Na segunda aparece das 22h até o fim do dia; **na terça aparece a
      madrugada 00h–06h** como continuação do turno de segunda.
- [ ] "Agora:" em cima mostra quem está de turno (conferir numa hora que
      tenha gente).
- [ ] Clicar na barra (admin): **troca** por outra pessoa — barra tracejada
      com "no lugar de". Só naquele dia; o dia seguinte continua normal.
- [ ] Troca **recortada** (ex.: só 02:00–06:00 do turno das 22h).
- [ ] **Folga**: barra cinza "Folga — Fulano", e "Agora" não conta a pessoa.
- [ ] "Desfazer exceção" devolve o turno.
- [ ] Colaborador vê a escala mas **não** cadastra nem clica para trocar.

### 3.9 Suítes (antes de qualquer deploy)
```bash
cd backend && npx jest
cd frontend && npx vitest run
```
Referência: backend **442 passando**, frontend **101**. As **6 falhas** em
`auth/auth-oauth.service.spec.ts`, `auth/auth.service.spec.ts` e
`users/users.service.spec.ts` são antigas. Uma sétima é regressão.

No Windows: `npx next build --webpack` (turbopack falha com node_modules em
link simbólico).

---

## 4. O que ficou por fazer

### Desenhado com o Erik, não construído
**Agendas → Manutenção** (terceira aba). Decisões já tomadas:
- Janela do cliente: empresa, início/fim, se repete (dias da semana) ou é
  avulsa, **de quem é a alteração (Alle/cliente)**, observações ("dicas":
  não mexer em tal coisa).
- Calendário de mudanças = **visão das GMUDs que já existem**, não cadastro
  novo. GMUD fora de qualquer janela do cliente aparece destacada.
- **Em aberto:** GMUD fora da janela **bloqueia** ou só **avisa**?
  Recomendação feita: só avisar (bloquear trava emergência de madrugada).
  O Erik ainda não respondeu.

**Indicadores** (quarta aba, futuro): disponibilidade e incidentes críticos
vindos do Zabbix, com retrato diário guardado. Travado pelo mesmo motivo do
Grafana: falta token por empresa.

### Pendências de antes (continuam)
- **Rotacionar `GRAPH_CLIENT_SECRET`** no Azure — exposto numa saída de
  terminal em 23/09. Dívida de segurança aberta.
- Compartilhar os três calendários do Outlook (dona: natalia.silva) com
  `suporte@alletecnologia` — sem isso a aba Plantão vem vazia.
- Reboot da VM (kernel novo instalado) — precisa de janela, a máquina roda
  produção.
- Três `COLLABORATOR` com empresa "Outros" (Rogério Carvalho, Rodrigo
  Colpani, Rangel Werner Lemos): da Alle ou de fora?
- "Só admin tira relatório mas está confundindo do adm" — frase nunca
  esclarecida.
- Oportunidades (kanban por e-mail): 7 perguntas em aberto.

### Resolvido sem precisar de código
- "Vazamento de usuário de cliente na tela de Apontamentos" (handoff de
  23/09): **não existe**. A lista da tela é por whitelist de papel
  (ADMIN/COLLABORATOR/PJ) em `rendimento.service.ts`.
- `fix/contraste-modo-claro`: já está todo na teste; a branch só está velha.

---

## 5. Em uma frase

Tudo de hoje está commitado na branch da teste e no `origin`, com os testes
automáticos verdes — e **nada foi visto rodando**; o próximo passo é deploy
da teste e a seção 3, antes de desenvolver mais.
