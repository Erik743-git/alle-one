# Buffers maiores para Cookie/Authorization (evita 400 Request Header Or Cookie Too Large).
# Incluir no bloco server { } do alleone (antes dos location).
client_header_buffer_size 16k;
large_client_header_buffers 8 32k;
